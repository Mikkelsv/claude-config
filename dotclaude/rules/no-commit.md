# Never Commit

Never create git commits. The user manages commits themselves. Do not offer to commit either.

**Exception:** Commits are allowed when working inside `~/Documents/Code/claude-config/` (the Claude config repo).
